{
  "tasks": {
    "dev": "deno run -A mod.ts run -S examples/script.ts -T $GITHUB_TOKEN -R 'kurtaking/octoherd-cli'",
    "compile": "deno compile -A --unstable mod.ts"
  },
  "imports": {
    "/": "./",
    "./": "./"
  }
}
